## aaron+ashley

Run this single command with your composed message inline:

```bash
~/.openclaw/skills/cron-creator/scripts/session-agent-turn.sh \
  "agent:main:bluebubbles:group:any;+;bc2201f817d34f7da609764bf73c4ffb" \
  --payload "YOUR COMPOSED MESSAGE HERE"
```

After the script completes, reply with exactly `NO_REPLY` and nothing else.

## aaron+direct

Run this single command with your composed message inline:

```bash
~/.openclaw/skills/cron-creator/scripts/session-agent-turn.sh \
  "agent:main:bluebubbles:direct:+15127407713" \
  --payload "YOUR COMPOSED MESSAGE HERE"
```

After the script completes, reply with exactly `NO_REPLY` and nothing else.

## aaron+sherri

Run this single command with your composed message inline:

```bash
~/.openclaw/skills/cron-creator/scripts/session-agent-turn.sh \
  "agent:sherri:bluebubbles:group:any;+;f0fed2fc707b4738a4d8fa06f9bf564a" \
  --payload "YOUR COMPOSED MESSAGE HERE"
```

After the script completes, reply with exactly `NO_REPLY` and nothing else.
